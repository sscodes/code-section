#include<stdio.h>
void main()
{
    int m,n,i,flag=0,j;
    printf("Enter the lower limit\n");
    scanf("%d",&n);
    printf("Enter the upper limit\n");
    scanf("%d",&m);
    for(i=n;i<=m;i++)
    {
        flag=0;
        for(j=2;j<=i/2;j++)
        {
            if(i%j==0)
               {
                flag=69;
                break;
               }

        }
        if(flag==0 && i!=1)
            printf("%d",i);
    }

    }
